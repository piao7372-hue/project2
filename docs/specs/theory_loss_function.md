# Theory Loss Function

Source PDF: `C:/Users/ASVS/Desktop/1/loss_function_final_explained.pdf`

与语义链路闭环的损失函数最终版（符号统一说
明稿）

## 1 先给结论
这份文稿对应的损失函数版本，已经过了一次专门的数学一致性检查。这里的“过
关”，指的是下面三件事同时成立：
˜ ˜
(1) 符号过关：全文统一使用 S 表示原始语义支持矩阵，使用 S = C S 表示最终监
督矩阵，不再出现 S e；
(2) 语义对象过关：跨模态监督只去约束跨模态关系，同模态监督不再错误地直接复
用跨模态矩阵，而是从 S 中诱导得到；
(3) 数学形式过关：每一项损失都有明确的定义域、值域、优化对象与作用目的，总
损失是非负的，而且除离散化映射 sgn (·) 不参与反向传播外，其余部分都可以连
续优化。
需要如实说明的是：这并不等于“实验效果已经被证明最优”。它的准确表述应当
是：这是一版数学上自洽、符号上统一、并且足够正式到可以写进论文方法段的主损失
函数。

## 2 记号、对象与总目标
设一个 mini-batch 中共有 N 对配对样本。图像模态与文本模态分别记为
I N T N
{x }, {x }.
i i=1 i i=1
前面的语义模块已经输出两个矩阵：
˜ N×N
- S ∈ [0,1] ：原始语义支持矩阵；

## 3 第一部分：由连续表示构造可比较的关系矩阵 2
N×N
- C ∈ [0,1] ：可信度矩阵。
于是最终监督矩阵定义为
˜
S = C S. (1)
˜
数学原理 Hadamard 逐元素乘法表示：对每一个跨模态样本对 (i, j)，先由 S ij 给出原
始语义强度，再由 C ij 给出该语义强度的可信度，最后逐元素相乘，得到训练时真正使
用的监督值 S ij。
含义 S ij 越大，表示图像样本 i 与文本样本 j 越应该在哈希空间中靠近；S ij 越小，表
示它们越不应该靠近。
用处 式 (1) 是整套损失函数与前面语义模块闭环的根。没有这一步，下游损失就无法
真正继承前面构造出的语义关系。
接下来，令哈希网络输出离散化前的连续表示
N×K N×K
H I ∈ R, H T ∈ R,
其中 K 是哈希码长度。与之对应的离散哈希码为
B I = sgn (H I), B T = sgn (H T).
训练阶段真正参与连续优化的是 H I, H T，而不是不可导的 B I, B T。

## 3 第一部分：由连续表示构造可比较的关系矩阵
为了把连续哈希表示映射为 [0,1] 区间上的关系分数，先对每个样本向量做行级 ℓ2
归一化。对任意样本 i，定义
I T
h h
̂ I i ̂ T i
h =, h =, (2)
i I i T
‖h ‖ + ε ‖h ‖ + ε
i 2 i 2
I T K
其中 h, h ∈ R 分别是 H I, H T 的第 i 行，ε > 0 是极小常数，用来避免分母为零。
i i
数学原理 归一化后，样本向量主要保留方向信息，模长影响被消去。这样两个样本之
间的内积就近似等于它们的余弦相似度，因此更适合用来表达“语义上是否接近”。
̂ I ̂ T
含义 h 与 h 是尺度归一后的表示，它们强调方向，而弱化绝对幅值。
i i

## 4 第二部分：不能直接拿 S 监督同模态关系，必须先诱导同模态目标 3
̂ ̂
用处 后续所有关系矩阵都建立在 H I,H T 之上，从而保证数值比较稳定，并使不同模
态的表示进入同一几何度量空间。
̂ ̂
由所有归一化后的样本向量组成矩阵 H I,H T，然后定义跨模态预测关系矩阵：
̂ ̂ ⊤
1 + H I H
T N×N
P IT = ∈ [0,1]. (3)
̂ ̂ ⊤
数学原理 H I H 的第 (i, j) 个元素就是图像样本 i 与文本样本 j 的余弦相似度，理
T
论范围在 [−1,1]。用线性映射 z 7→ (1 + z)/2 将其移到 [0,1] 区间，就能与监督矩阵
N×N
S ∈ [0,1] 直接比较。
含义 P IT,ij 表示模型当前认为图像 i 与文本 j 应该有多接近。它是模型视角下的跨模
态关系预测值。
用处 式 (3) 是后面跨模态关系损失的预测对象。没有它，就无法把连续哈希表示与监
督矩阵 S 联系起来。
同理，定义同模态内部的预测关系矩阵：
̂ ̂ ⊤ ̂ ̂ ⊤
1 + H I H 1 + H T H
I T
P II =, P T T =. (4)
2 2
数学原理 这里仍然是“余弦相似度 + 线性平移”的同一构造，只是关系对象从跨模
态换成了同模态，因此得到的是图像内部关系预测与文本内部关系预测。
含义 P II,ij 衡量模型当前认为图像 i 与图像 j 有多接近；P T T,ij 对文本模态同理。
用处 后面如果只拟合 P IT 而不管 P II, P T T，模型可能只学会“图文之间怎么配”，却
学不会“每个模态内部如何组织”。因此这两个矩阵是必要的，而不是可有可无的。

## 4 第二部分：不能直接拿 S 监督同模态关系，必须先诱导
同模态目标
这里必须明确指出一个数学原则：跨模态监督矩阵 S 不能直接用来监督 P II 或
P T T。
原因在于，S ij 描述的是“图像 i 与文本 j 的关系”，而 P II,ij 与 P T T,ij 描述的分别
是“图像与图像的关系”以及“文本与文本的关系”。它们属于不同的语义对象，不能
直接混用。

## 5 第三部分：加权语义关系损失 4
⊤
因此，需要先从 S 中诱导出同模态目标矩阵。做法如下：先对 S 的行向量以及 S
的行向量做 ℓ2 归一化：
⊤
S (i,:) S (i,:)
I T
q =, q =. (5)
i i ⊤
‖S (i,:)‖ + ε ‖S (i,:)‖ + ε
2 2
令
   
I ⊤ T ⊤
(q) (q)
1 1
   
..
.  . 
Q I =., Q T =..
   
I ⊤ T ⊤
(q) (q)
N N
再定义
⋆ ⊤ ⋆ ⊤
S = Q I Q, S = Q T Q. (6)
II I T T T
⊤
数学原理 S (i,:) 可以看作图像样本 i 相对于全部文本样本的“关系轮廓”；S (i,:) 可
以看作文本样本 i 相对于全部图像样本的“关系轮廓”。如果两个图像样本的关系轮廓
相似，则它们在图像模态内部也应更接近；文本模态同理。式 (6) 用归一化轮廓之间的
余弦相似度来定义这种同模态接近性，因此在数学上是顺的。
⋆ ⋆
含义 S 描述“图像 i 与图像 j 是否在跨模态语义关系上表现相似”；S 描述文
II,ij T T,ij
本模态的对应情况。
用处 这是整套损失函数里非常关键的一步。它避免了语义对象错位的问题，也让同模
态监督不是凭空捏造出来的，而是确实来自前面已经构造好的最终监督矩阵 S。

## 5 第三部分：加权语义关系损失
为了让强语义关系在训练中发挥更大作用，同时又不把弱语义关系完全丢掉，定义
三个逐元素权重矩阵：
⋆ ⋆
W IT = 1 + βS, W II = 1 + βS, W T T = 1 + βS, (7)
II T T
其中 β ≥ 0 是加权强度，1 表示与对应矩阵同型的全 1 矩阵。
数学原理 目标值本身越大，说明对应样本对越重要，那么该项误差在损失中也应该占
更大权重。加上常数矩阵 1 的作用，是保证低权重样本不会被完全忽略。
含义 W IT,ij, W II,ij, W T T,ij 分别表示对应关系项在训练中的“重要程度”。

## 5 第三部分：加权语义关系损失 5
用处 它们负责让优化器把更多注意力放在高可信、高语义强度的关系上，但同时保留
整个矩阵的全局信息。
基于这些权重，定义跨模态关系拟合项：
∥ ∥
∥ 1/2 ∥
∥W (P IT − S)∥
IT
F
L IT =. (8)
‖W IT‖
数学原理 这里是一个带权的 Frobenius 范数回归项。P IT − S 是逐元素误差矩阵，左
边乘上逐元素平方根权重，相当于把重要位置的误差放大，再对全矩阵做平方和。最后
除以 ‖W IT‖ ，是为了把损失量级规范化，避免 batch 规模或权重总量变化造成数值漂
移。
含义 L IT 越小，说明模型预测的跨模态关系矩阵 P IT 越接近最终监督矩阵 S。
用处 这是整套方法的主监督项，它直接负责把跨模态语义关系压到连续哈希表示中。
同理，定义同模态拓扑拟合项：
∥ ∥ ∥ ∥
2 2
∥ 1/2 ⋆ ∥ ∥ 1/2 ⋆ ∥
∥W (P II − S)∥ ∥W (P T T − S)∥
II II T T T T
F F
L II =, L T T =. (9)
‖W II‖ ‖W T T‖
1 1
数学原理 L II 与 L T T 与 L IT 在形式上完全同构，只是监督目标分别换成了图像拓扑
⋆ ⋆
目标 S 与文本拓扑目标 S 。
II T T
含义 L II 越小，说明图像模态内部的关系结构越接近由 S 诱导出的目标结构；L T T 对
文本模态同理。
用处 它们补齐了“只有跨模态监督，没有同模态拓扑”的缺口，使模型学到的哈希空
间不仅能做图文匹配，还具有更稳定的模态内部组织结构。
于是定义语义关系总损失为
α
L sem = L IT + (L II + L T T), (10)
其中 α ≥ 0 控制同模态拓扑约束的强度。
数学原理 这里采用“主项 + 辅项”的结构。由于最终监督矩阵 S 本身是跨模态矩阵，
所以 L IT 是主项；L II 与 L T T 虽然重要，但它们只是从 S 中派生出来的结构监督，因
此用系数 α 控制其强度是合理的。

## 6 第四部分：配对一致性损失 6
含义 L sem 越小，说明模型同时学好了跨模态关系与由此引出的同模态结构。
用处 这就是与前面语义模块闭环最紧的一项，也是整套损失函数的核心。

## 6 第四部分：配对一致性损失
I T
虽然 L IT 已经在全体图文对上施加了监督，但对于配对样本 (x, x)，仍然需要一
i i
个更直接的实例级拉近约束。定义
∥ ∥
1 ∥ ∥
̂
L pair = ∥̂H I − H T∥. (11)
N F
∥ ∥
∥ ∥
̂ ̂ ̂
数学原理 由于 H I,H T 是逐行归一化的矩阵，∥̂H I − H T∥ 就是各配对样本在单位球
F
附近的平方欧氏距离总和。对于单位向量，这一距离与余弦相似度单调对应，因此它本
质上是在最大化配对样本的角度一致性。
含义 L pair 越小，说明每一对原始图文样本在连续哈希空间中越对齐。
用处 这项损失给每一对样本一个实例级锚点，防止模型只在“整体关系矩阵拟合”上
做得好，却没有真正绑紧每一对配对样本。

## 7 第五部分：量化损失
哈希学习的最终目标不是任意实值向量，而是接近 {−1,+1} 的二值码。因此需要
把连续表示推向二值顶点。这里采用一个平滑的量化项：
1 ()
2 2
L q = ‖H I H I − 1‖ + ‖H T H T − 1‖. (12)
F F
N K
数学原理 对任意标量 h，若希望它接近二值集合 {−1,+1}，那么等价地希望 h 接近
2 2
1。因此用 (h − 1) 来惩罚偏离即可。矩阵形式下，H I H I − 1 与 H T H T − 1 恰好
逐元素刻画了这一偏离量，而且该表达式相对于直接使用 sgn (·) 更平滑，可用于连续优
化。
含义 L q 越小，说明连续表示的每一个分量越接近 ±1，也就意味着后续二值化造成的
失真越小。

## 8 第六部分：比特平衡损失 7
用处 这项损失负责把“好看的连续表示”进一步推向“真正适合做哈希码的近二值表
示”。没有它，训练阶段学得很好，测试时一做符号函数离散化就可能掉性能。

## 8 第六部分：比特平衡损失
除了要接近二值顶点，还要防止某些 bit 在整个 batch 中长期偏向全 +1 或全 −1。
为此定义比特平衡项：
∥ ∥ ∥ ∥
2 2
1 ∥ 1 ∥ 1 ∥ 1 ∥
∥ ⊤ ∥ ∥ ⊤ ∥
L bal = 1 H I + 1 H T. (13)
∥ ∥ ∥ ∥
K N K N
2 2
数学原理 若某一 bit 在 batch 中长期偏正或偏负，那么该 bit 的样本均值就会远离 0。
因此，只要惩罚每个 bit 的 batch 均值偏离 0 的程度，就能鼓励正负符号在样本维度上
更平衡。
含义 L bal 越小，说明各 bit 在整个 batch 中越接近均衡使用。
用处 这项损失可以避免无效位，提升哈希码容量利用率，减少大量样本落在相似编码
模式上的风险。

## 9 第七部分：总损失函数
把以上各项组合起来，定义最终总损失为
L = λsem L sem + λpair L pair + λq L q + λbal L bal, (14)
其中
α
L sem = L IT + (L II + L T T). (15)
数学原理 总损失是若干非负项的加权和，因此本身也是非负的。每一项都承担不同但
互补的功能：
- L sem：负责关系保持，是主项；
- L pair：负责配对样本对齐，是实例级约束；
- L q：负责逼近二值顶点，是量化约束；
- L bal：负责避免 bit 退化，是容量利用约束。
因此这不是任意拼接，而是一个功能分工明确的联合目标。

## 10 第八部分：为什么说这版是“过关”的 8
含义 式 (14) 表示：模型训练时既要忠实拟合前面给出的最终语义监督矩阵 S，又要
维持实例对齐、近二值性与编码平衡性。
用处 这就是可以直接落到实现与论文方法段里的最终主损失函数。

## 10 第八部分：为什么说这版是“过关”的
最后把“为什么说它过关”再明确总结一次。
1. 符号过关
全文的关键链路始终是
C
˜
S −→ S,
没有再混入 S e 这种旧符号。上游语义输出、下游损失监督、同模态目标诱导，全都围
绕同一套记号展开。
2. 语义对象过关
跨模态矩阵 S 只直接监督跨模态预测矩阵 P IT；同模态预测矩阵 P II, P T T 则通过
⋆ ⋆
S, S 间接监督。这种“对象对对象”的约束关系是正确的，没有语义错位。
II T T
3. 数学形式过关
所有核心损失项都是平方范数型非负函数；行归一化里有 ε 保证稳定；量化项没有
直接把不可导的 sgn (·) 塞进训练目标，而是采用平滑替代。因此从连续优化的角度看，
这版是可以成立的。
4. 与前面思想闭环过关
最重要的一点是：这版损失不是另起炉灶，而是直接把前面构造好的最终监督矩阵
S 作为主线，把它进一步传播为同模态结构约束，再配上哈希学习必须具备的量化和平
衡约束。因此从研究叙事上，它和你前面的思想是环环相扣的。

## 11 最后一句话 9

## 11 最后一句话
如果只问一句话结论，那就是：
这版损失函数已经不是“先凑一个能跑的式子”，而是一版符号统一、对象
正确、数学上顺、并且能正式写进论文的主损失函数。
但仍然要保持学术上的诚实：数学过关不等于实验已经证明最优。后者仍然需要靠
后续实验来确认。
